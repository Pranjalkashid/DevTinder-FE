change light- dark theme using daisy UI

12.1.1.0 -ip address is same as localhost

some time the server makes cache so we restart the terminal and it workd fine
how do we handle this in production

Season 3 - 12 hrs
Testings there are some bugs

1. for non logged in user - home page ? - chat gpt options implement later
2. after 10 users shown in feed we have to refresh to get next 10 - fix this this should be done automatically
3. user card - frame-motion
4. UI looks boring - all white - add colours

Looks good :

1. sign in
2. log in
3. profile page - view and edit
4. feed
5. connections
6. requests received
7. log out
